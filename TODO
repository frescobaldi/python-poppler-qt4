TODO:

- fix Poppler::HighlightAnnotation::Quad.points, causes runtime errors
- create a few Pythonic convenience interfaces for e.g. iterating over fonts, etc.

DONE:
- cast returned lists of objects like Document::links() to their respective types
- convert to and from QLinkedList

